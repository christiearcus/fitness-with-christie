# Fitness with Christie

- Do a randomized HIIT workout
- Do a randomized stretch

## Local development

`brew install node`
`npm install yarn`
`yarn install`
`yarn start`

## Deployment

`yarn predeploy`
`yarn deploy`
